models:
  Branch: 
    name: string
    code: string
    description: string
    is_patrimonial: boolean
    relationships:
      hasMany: Product
  Product:
    name: string
    code: string
    description: longtext
    price: longtext
    is_company: boolean
    branch_id: id
 
seeders: Branch, Product