models:
  Product:
    name: string
    code: string
    description: longtext
    price: longtext
    is_company: boolean
    branch_id: id
  Branch: 
    name: string
    code: string
    description: string
    is_patrimonial: boolean
    relationships:
      hasMany: Product
seeders: Branch, Product